# Lambda function to calculate area of rectangle
area = lambda length, breadth: length * breadth

# Take input from user
l = float(input("Enter length: "))
b = float(input("Enter breadth: "))

# Print the result
print("Area of rectangle =", area(l, b))

    
#type by sir simple lambda function code
# area_of_rect = lambda l, b: l * b
# print(area_of_rect(4, 8))
